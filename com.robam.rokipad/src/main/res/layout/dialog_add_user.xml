<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <RelativeLayout
        android:id="@+id/layout"
        android:layout_width="@dimen/dialog_add_user_width"
        android:layout_height="@dimen/dialog_add_user_height"
        android:layout_gravity="center"
        android:background="@color/dialog_choose_bg" >
        <ImageView
            android:id="@+id/img_qrcode"
            android:layout_width="256dp"
            android:layout_height="256dp"
            android:layout_alignParentTop="true"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="22dp"
            android:scaleType="fitXY"
            android:contentDescription="@null"
            android:src="@mipmap/img_qrcode" />

        <TextView
            android:id="@+id/tv_hint"
            android:layout_width="match_parent"
            android:layout_height="@dimen/prepare_dialog_hint_height"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:background="@color/home_bg"
            android:gravity="center"
            android:text="@string/dialog_add_user_hint"
            android:textColor="@color/White"
            android:textSize="@dimen/dialog_add_user_hint_size" />

    </RelativeLayout>

</FrameLayout>